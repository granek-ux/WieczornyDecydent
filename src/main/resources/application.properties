spring.application.name=WieczornyDecydent


# === PO??CZENIE Z BAZ? POSTGRESQL ===
# Zmie? port, nazw? bazy, u?ytkownika i has?o na swoje
spring.datasource.url=jdbc:postgresql://localhost:5432/wieczorny_decydent_db
spring.datasource.username=postgres
spring.datasource.password=twoje_tajne_haslo

# === KONFIGURACJA JPA (HIBERNATE) ===
# To jest KLUCZOWY element podej?cia "Code-First" z Flyway!
# "validate" mówi Hibernate: "NIE twórz ani NIE zmieniaj tabel.
# Sprawd? TYLKO, czy encje (@Entity) pasuj? do tego, co JEST w bazie."
spring.jpa.hibernate.ddl-auto=validate

# Pokazuje SQL generowany przez JPA w konsoli (super do debugowania)
spring.jpa.show-sql=true

# === KONFIGURACJA FLYWAY ===
# Flyway jest domy?lnie w??czony, bo doda?e? zale?no??.
# Ta linia jest opcjonalna, ale dobra dla jasno?ci.
spring.flyway.enabled=true